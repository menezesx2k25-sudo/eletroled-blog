# Blog www redirect Worker

Redirects the non-canonical blog host to the canonical blog URL:

- From: `https://www.blog.consertoeletroled.com/*`
- To: `https://blog.consertoeletroled.com/*`

Deploy:

```powershell
npx wrangler deploy
```
