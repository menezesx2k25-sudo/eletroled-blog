name: Publicar artigo agendado

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/publish-scheduled.yml'
      - 'package.json'
      - 'scripts/publish-scheduled.mjs'
      - 'scripts/content-quality.mjs'
      - 'scripts/validate.mjs'
      - 'scripts/audit-generated.mjs'
      - 'tests/**'
      - 'content/publishing-approvals.json'
  schedule:
    # 08:20 em Santos (America/Sao_Paulo, UTC-3), de segunda a sexta.
    - cron: '20 11 * * 1-5'
  workflow_dispatch:
    inputs:
      count:
        description: 'Quantidade de artigos para publicar manualmente'
        required: false
        default: '1'
      once_per_day:
        description: 'Impedir segunda publicação na mesma data'
        required: false
        default: 'false'

permissions:
  contents: write

concurrency:
  group: publish-scheduled-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar repositório
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Executar preflight editorial e SEO
        run: npm run check

      - name: Publicar próximo rascunho
        id: publish_draft
        env:
          PUBLISH_COUNT: ${{ github.event.inputs.count || '1' }}
          PUBLISH_ONCE_PER_DAY: ${{ github.event.inputs.once_per_day || 'false' }}
        run: |
          EXTRA_ARGS=""
          if [ "${{ github.event_name }}" = "schedule" ]; then
            EXTRA_ARGS="--once-per-day"
          elif [ "${{ github.event_name }}" = "push" ]; then
            EXTRA_ARGS="--dry-run --once-per-day"
          elif [ "$PUBLISH_ONCE_PER_DAY" = "true" ]; then
            EXTRA_ARGS="--once-per-day"
          fi

          npm run publish:scheduled -- --count="$PUBLISH_COUNT" $EXTRA_ARGS

      - name: Gerar site estático
        if: steps.publish_draft.outputs.published_count != '0'
        run: npm run build

      - name: Validar SEO e arquivos gerados
        if: steps.publish_draft.outputs.published_count != '0'
        run: npm run validate

      - name: Auditar HTML gerado
        if: steps.publish_draft.outputs.published_count != '0'
        run: npm run audit:generated

      - name: Verificar integridade do diff
        if: steps.publish_draft.outputs.published_count != '0'
        run: git diff --check

      - name: Commitar publicação
        if: steps.publish_draft.outputs.published_count != '0'
        run: |
          if git diff --quiet; then
            echo "Nenhuma alteração para publicar."
            exit 0
          fi

          git config user.name "EletroLED SEO Bot"
          git config user.email "actions@github.com"
          git add content/posts.json content/drafts.json dist scripts package.json
          git commit -m "Publish scheduled blog post"
          git push